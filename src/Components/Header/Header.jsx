import Navbar from './Navbar';

const Header = () => {
    return (
        <div className="p-1 bg-gradient-to-r from-blue-700 to-red-500 rounded-lg">
            <Navbar />
        </div>

    );
};

export default Header;